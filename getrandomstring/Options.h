#pragma once

struct Options
{
    bool showHelp;
    bool excludeNumbers;
    bool excludeLowercase;
    bool excludeUppercase;
    bool excludeSpecial;
    bool useLetterFrequency;
    size_t length;
};
